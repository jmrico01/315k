#pragma once

#include <km_common/km_defines.h>

#define APP_315K

global_var const char* APP_NAME = "315k";
global_var const uint64 PERMANENT_MEMORY_SIZE = MEGABYTES(256);
global_var const uint64 TRANSIENT_MEMORY_SIZE = GIGABYTES(1);
